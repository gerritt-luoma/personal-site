import { Flex, TextInput } from "@mantine/core";
import { useForm } from "@mantine/form";


const SignInPage = () => {
    return (
        <Flex>
        </Flex>
    );
};

export default SignInPage;